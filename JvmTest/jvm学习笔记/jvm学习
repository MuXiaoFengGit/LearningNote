1、栈与栈帧
	1）栈中每调用一个方法，就会新建一个栈帧，并push进栈中，方法执行完毕后，return 数据先保存在栈帧的局部变量区中，
再执行Finally块，然后将栈帧pop出来，也就是说，一个栈中包含多个栈帧。
	2）堆：创建对象    
		堆包含新生代（Eden）,老年代（S0 S1）(from to)
	   方法区：永久块，包含类信息，常量池，常量
	3）直接内存：NIO涉及，可直接访问，比堆快。
	4）栈帧包含：局部变量、方法参数、返回值等
	5）本地方法栈、PC寄存器
	6）GC回收包括堆，方法区(废弃常量、无用的类)，直接内存。
		
	
2、jvm参数配置
	1）-XX 表示配置系统级信息，如GC日志打印，回收器类型
	  其他如-Xms是应用级信息，如内存，栈，堆大小
	  +表示启用  -表示禁用
	2)-XX:+PrintGC 每次GC都打印日志
	  -XX:+UseSerialGC 使用串行回收器
	  -XX:+PrintGCDetails 打印GC详细信息，包含堆详细使用情况
	  -Xms5m -Xmm20m jvm使用堆最小5M最大值20M 
	
3